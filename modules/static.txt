Можно ли обращаться к статическому полю или методу не используя имя класса?QQQ+QQQМожно, так как статические члены класса напрямую принадлежат классу, а не его экземпляру.
!!!!!
Мы можем получить доступ к нестатическим членам класса, внутри статического контекста?QQQ-QQQОшибка компиляции. Мы не можем получить доступ к нестатическим членам класса, внутри статического контекста.
!!!!!
Можно ли переопределять static методы?QQQ-QQQметод, marked static неявно маркается как final, поэтому переопределеня и быть не может. На них не распространяется полиморфизм в runtime, они связыаваются на этапе компиляции.
!!!!!
Можно ли объявить такой же static метод с такой же сигнатурой в классе наследнике, как и static метод в базовом классе?QQQ+QQQДа, hidding methods. Мы всего лишь спрячем метод суперкласса вместо переопределения. Во время компиляции всегда будет вызван метод исходя из типа переменной.
!!!!!
Можно ли объявить static класс, за исключением верхнего уровня?QQQ+QQQNested(static) class. Их полезность - предоставление улучшенных связей. Пример HashMap.Entry.
!!!!!
Static initializer block будет выполнен во время загрузки класса?QQQ+QQQЕсли не объявим такой блок, то JVM сама соберет все static поля и выполнит его во время загрузки класса.
!!!!!
static методы связываются во время компиляции, а не static методы связываются во время runtime?QQQ+QQQСтатческие-во время компиляции, не статически в runtime.
!!!!!
static поля сериализуются?QQQ-QQQВо время сериализации static поля, как и поля transient не сериализуются. После десериализации новый объект будет содержать первичное значение, если int = то 0, если float = то 0.0, если object = то null.
!!!!!
static import позволяет позволяет импортировать static члены класса?QQQ+QQQДоступ к полям без указания имя класса, к методам, как будто они определены в этом классе.
!!!!!
В статике может использоваться this!?QQQ-QQQВ статике не может быть никакого this.
!!!!!
